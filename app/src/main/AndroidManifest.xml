<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.apparisan">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/logo_fitmi"
            android:supportsRtl="true"
            android:theme="@style/NoAppTheme">
        <activity android:name=".view.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity> <!-- <activity android:name=".view.BerandaActivity"> -->
        <!-- </activity> -->
        <activity android:name=".auth.AuthActivity">
        </activity>
        <activity android:name=".view.EditProfilActivity"/>
        <activity
                android:name=".view.DashboardActivity"
                android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".view.ProfilActivity"
                  android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>